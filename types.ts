export interface NavLink {
  label: string;
  href: string;
}

export interface StatItem {
  value: string;
  label: string;
}

export interface SolutionCardProps {
  title: string;
  description: string;
  imageSrc: string;
  linkText: string;
}
