import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  base: "/bravink-preview-site/", // Ensures assets are linked correctly on GitHub Pages
});
